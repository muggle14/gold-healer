# Legacy service artwork

This folder contains 25 historical gold service illustrations totalling approximately
69 MB. The current service cards use line icons, so these images are retained for
history and are not loaded by the website.

Do not delete these files. If they are formally superseded, move the folder to
`archive/` with `git mv` in a separately approved change.
