# Phase 0–6 rollback archive

These files were introduced after checkpoint `2fb7393` and were withdrawn on
2026-08-08 at the website owner's request.

The active website and its original support files were restored from `2fb7393`.
The custom-domain `CNAME` was retained because it came from `main` independently of the
redesign. Historical phase documents, generated brand assets, staging page, validation
tools, and vendored dependencies remain here so the rollback is recoverable.
