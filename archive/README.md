# Archive

Superseded artefacts with history value belong here. Move them with `git mv`; do not
delete them from history.

- `rollback-2026-08-08/` preserves the Phase 0–6 implementation that was withdrawn when
  the website was restored to checkpoint `2fb7393`.
