# Scripts

Small validation utilities for maintaining the static website live here.

`validate_site.py` checks the staged or production homepage, local asset references,
structured data, hidden-service exclusions, testimonial status, policy URLs, and the
canonical sitemap before publication.

